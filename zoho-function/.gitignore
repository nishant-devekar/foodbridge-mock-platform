.env
.env.*
!.env.example
.vercel
node_modules
.env*

# Written by the local feedback store (dev only); the deployed bridge uses KV.
feedback.jsonl
# Store Builder builds kept by the local stores store (dev only): real customer data, never committed.
stores-data/
