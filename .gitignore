# generated by tools/dev.py — points at local checkouts, never canonical
assets/modules.local.json

# agent/editor run configuration — machine-specific, and the sibling-repo entry
# assumes a checkout most people will not have. tools/dev.py is the documented
# entry point (see DEVELOPMENT.md).
.claude/
